# SBC-7B2 FT3 Frontend Dependency Admission

Date: 2026-09-16
Status: ADMITTED FOR FT3/FT4 DEVELOPMENT / EXACT PINS REQUIRED
Protected entry main: `ebf1ae9d0e10f0f427625d4c3ab5c1703299d154`

## Exact direct dependencies

Runtime/source:
- `vue` `3.5.42` — MIT
- `reka-ui` `2.10.4` — MIT
- `@tanstack/vue-table` `9.2.4` — MIT

Build/development:
- `vite` `8.3.0` — MIT
- `@vitejs/plugin-vue` `6.0.9` — MIT
- `typescript` `7.0.2` — Apache-2.0
- `vue-tsc` `3.3.11` — MIT

Vite 8 requires Node 20.19+ or 22.12+. SharkBooks sets the FT3 source engine floor to Node `>=22.12.0`.

## Admission rules

- exact package versions only, with no `^` or `~`;
- npm package-lock must be generated by npm and committed before candidate freeze;
- do not fabricate lockfile integrity values;
- no CDN/runtime network dependency;
- capture direct/transitive package identity and licence evidence before candidate freeze;
- Reka provides accessible primitives only and owns no accounting semantics;
- TanStack Table is presentation/view state only and owns no accounting semantics;
- shadcn-vue remains selective reviewed source-porting only, not an admitted floating runtime dependency;
- the Shark mini Action renderer remains Shark-owned source;
- the existing Tauri bounded IPC authority is retained; no generic executor is admitted.

## Initial scaffold classification

The first `workspace/ui` commit is build/source scaffold only. It preserves the existing create/open/verify shell operations and freezes the SBC-7A navigation in Vue. Other sections remain visibly unwired until their exact Action bindings are implemented and proven.

This document does not freeze an integration candidate or authorise merge.
