# SBC-7B2 FT3A + FT3B + FT4 — Integrated Implementation Contract

Date: 2026-09-16
Status: ACTIVATED / IMPLEMENTATION CONTRACT / NO CANDIDATE FROZEN
Protected entry main: `ebf1ae9d0e10f0f427625d4c3ab5c1703299d154`

## Authority

This contract continues the frozen SBC-7A owner UI contract and the merged FT1+FT2 Action System. It is governed by programme authorities 129, 130, 167, 168/168A/168B/168C, 171/171A, 173, 185, 186, 187 and 188.

The lane intentionally integrates FT3A, FT3B and FT4 into one substantial candidate where practical, followed by one Windows and one Apple same-SHA Super-Gate.

## Frozen navigation

`Home | Money in | Money out | Bank | Receipts | Contacts | Reports | Settings`

The owner UI must not expose journals, debit/credit mechanics, Beankeeper, SQLite/SQLCipher internals, raw database paths, generic shell/filesystem authority, or professional/tax conclusions.

Vue/TypeScript is presentation and orchestration only. Shark/Rust remains semantic and execution authority. Every executable owner control must map to an admitted Action ID and bounded application operation.

## FT3A — UI-A

Implement:
- application shell;
- Home;
- deterministic Attention surface;
- Money In;
- Money Out;
- Bank activity/import/review/match/reconciliation surfaces;
- Action-bound controls;
- desktop/iPad/iPhone responsive behaviour and accessibility relevant to these screens.

## FT3B — UI-B

Implement:
- Receipts/Documents;
- Contacts;
- Reports;
- Settings/storage;
- admitted read/view gaps below;
- desktop/iPad/iPhone responsive behaviour and accessibility.

## FT4 — finite command/text surface

Implement a finite text-command route over the same Action Registry/controller used by manual controls.

Rules:
- currently available eligible actions may route to the same bounded owner operations as manual controls;
- future/locked actions remain registered but fail closed as unavailable;
- missing slots enter deterministic clarification;
- confirmation class and state reread cannot be weakened;
- text route cannot gain authority beyond manual UI;
- the 175 canonical voice-eligible IDs and 700 initial utterance fixtures remain the parity corpus;
- speech-to-text transport is not part of this lane.

## Critical-gap disposition

Implement now as minimal owner-safe read/view bridges:
- `MONEY.RECORDS.LIST`
- `MONEY.RECORD.DETAIL`
- `BANK.ACTIVITY_DETAIL`
- `DOCUMENT.LIST`
- `DOCUMENT.OPEN_VIEW`

These additions may expose only factual/owner-safe read shapes and must not introduce accounting judgement.

Explicitly defer/fail closed in this lane:
- `BOOKS.OPENING_BALANCES`
- `OWNER.CONTRIBUTION.RECORD`
- `MONEY_OUT.PERSONALLY_PAID_BUSINESS_EXPENSE`
- `OWNER.DRAWING.RECORD`
- `MONEY.TRANSFER.BANK_CASH`
- `CAMERA.CAPTURE_RECEIPT`

The UI must not simulate a deferred action using semantically different behaviour.

All SBC-8 incubated capabilities remain production locked.

## Frontend build boundary

Protected main currently serves `workspace/dist` through Tauri `frontendDist: "../dist"`.

Add governed frontend source at `workspace/ui/` and build production output to `workspace/dist/`. Do not change the Tauri frontendDist boundary unless a concrete defect requires separate review.

Use the existing bounded `window.__TAURI__.core.invoke` seam. Do not add a generic command executor or arbitrary command-name forwarding.

## Exact frontend dependency admission

Exact stable pins only:
- `vue` `3.5.42` — MIT
- `vite` `8.3.0` — MIT
- `@vitejs/plugin-vue` `6.0.9` — MIT
- `reka-ui` `2.10.4` — MIT
- `@tanstack/vue-table` `9.2.4` — MIT

No caret/tilde ranges. No CDN/runtime network requirement. The npm lockfile must be generated by npm and committed; it must not be fabricated manually. Capture transitive identities/licence evidence before candidate freeze.

`shadcn-vue` is not admitted as a floating runtime dependency here. Only selective reviewed source-porting with provenance is permitted. The Shark mini Action renderer is Shark-owned source.

## Required source separation

Keep separate modules for:
- exact Tauri IPC client;
- generated Action contract/types;
- Action binding metadata;
- deterministic command/clarification surface;
- screen view models;
- reusable accessible primitives;
- table/list and mobile-card presentations;
- confirmation UI driven by registry confirmation class.

## Continuous development proof

During implementation run bounded proof continuously:
- Action binding/static checks;
- frontend unit/component tests;
- accessibility/keyboard assertions;
- responsive checks;
- exact npm dependency/lock checks;
- focused Rust tests for new read/view bridges;
- affected inherited regressions.

Do not spend an Apple session after every UI slice unless a native/platform change requires it.

## Integration Super-Gate

The eventual immutable candidate must cover:
- SG0 preflight;
- SG1 Action Stack/generator contract;
- SG2 controller/Attention;
- SG3 FT3A UI-A;
- SG4 FT3B UI-B;
- SG5 finite command/text/voice parity;
- SG6 inherited regressions;
- SG7 platform compile where required;
- SG8 final SHA/lock/repository integrity.

Repairs create a new candidate SHA and require affected proof reruns.

## Hard boundaries

Not authorised in this lane:
- VAT/CIS/payroll/company/partnership accounting;
- tax calculation/advice/reserve logic;
- direct HMRC submission;
- Open Banking/live feeds;
- cloud OCR requirement;
- AI/LLM accounting judgement;
- raw SQL/database authority in UI;
- generic shell or broad filesystem access;
- silent match/reconciliation confirmation;
- SBC-8 production activation;
- speech runtime;
- merge without a later governed same-SHA evidence/owner gate.
